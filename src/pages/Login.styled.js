import styled from '@emotion/styled';

export const LoginWrapper = styled.div`
  position: relative;
  padding: 50px 0;
  text-align: center;
  & form {
    display: flex;
    flex-direction: column;
    row-gap: 40px;
  }
  & button {
    margin: 0 auto;
  }
`;
